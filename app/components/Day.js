var React = require('react');
var PropTypes = require('prop-types');

class Day extends React.Component {
    constructor(props) {
        super(props)
    }
}

Day.propTypes = {
    
}

module.exports = Day;